#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
     char a;
    scanf("%c",&a);
    if(a>=65&&a<=98)
    {
        printf("Upper");
    }
    else
        if(a>=97&&a<=122)
        {
            printf("Lower");
        }
    else
        if(a>=48&&a<=57)
        {
            printf("Number");
        }
    else
        if(a>=33&&a<=43)
    {
        printf("Symbol");  
        }
    return 0;
}
